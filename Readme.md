#McAfee IPS SSH Scraper
```
Need to setup GOPATH
```
##export GOPATH=<current directory>

##To Execute
```
go run main.go -hostfile=sensors.csv -user=username -password=password -cmd=./showme.bash
```